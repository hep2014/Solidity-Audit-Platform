services:
  postgres:
    image: postgres:16-alpine
    container_name: solidity-audit-postgres
    environment:
      POSTGRES_USER: audit
      POSTGRES_PASSWORD: audit
      POSTGRES_DB: audit
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U audit -d audit"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    container_name: solidity-audit-redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  backend-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: solidity-audit-backend-api
    env_file:
      - ./backend/.env
    environment:
      RUN_MIGRATIONS: "true"
      DOCKER_CONTAINER_STORAGE_DIR: /app/storage
      DOCKER_HOST_STORAGE_DIR: /home/hep2014/PycharmProjects/solidity/backend/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - ./backend/storage:/app/storage
      - /var/run/docker.sock:/var/run/docker.sock

  backend-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: solidity-audit-backend-worker
    env_file:
      - ./backend/.env
    environment:
      DOCKER_CONTAINER_STORAGE_DIR: /app/storage
      DOCKER_HOST_STORAGE_DIR: /home/hep2014/PycharmProjects/solidity/backend/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend-api:
        condition: service_started
    volumes:
      - ./backend/storage:/app/storage
      - /var/run/docker.sock:/var/run/docker.sock
    command:
      [
        "celery",
        "-A",
        "app.workers.celery_app.celery_app",
        "worker",
        "--loglevel=info",
        "--concurrency=1"
      ]

  frontend:
    image: node:22-alpine
    container_name: solidity-audit-frontend
    working_dir: /app
    depends_on:
      backend-api:
        condition: service_started
    environment:
      VITE_API_BASE_URL: http://localhost:8000
      VITE_WS_BASE_URL: ws://localhost:8000
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    command: sh -lc "npm install && npm run dev -- --host 0.0.0.0"

  slither-analyzer:
    build:
      context: ./analyzers/slither
      dockerfile: Dockerfile
    image: solidity-audit-slither:latest
    profiles:
      - analyzers

  mythril-analyzer:
    build:
      context: ./analyzers/mythril
      dockerfile: Dockerfile
    image: solidity-audit-mythril:latest
    profiles:
      - analyzers

  foundry-analyzer:
    build:
      context: ./analyzers/foundry
      dockerfile: Dockerfile
    image: solidity-audit-foundry:latest
    profiles:
      - analyzers

  echidna-analyzer:
    build:
      context: ./analyzers/echidna
      dockerfile: Dockerfile
    image: solidity-audit-echidna:latest
    profiles:
      - analyzers

volumes:
  postgres_data:
  redis_data:
  frontend_node_modules:
